<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Unraid Template Manager" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-900 min-h-screen text-gray-100">
    <!-- Navigation -->
    <nav class="bg-gray-800 shadow-sm border-b border-gray-700">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <%= link_to root_path, class: "text-xl font-bold text-white" do %>
              UnRAID Template Manager
            <% end %>
          </div>
          <div class="flex items-center space-x-4">
            <%= link_to "Templates", templates_path, class: "text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
            <%= link_to "Comparisons", template_comparisons_path, class: "text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
            <%= link_to "Sync Jobs", sync_job_runs_path, class: "text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash messages -->
    <% if notice %>
      <div data-controller="flash-message"
           data-flash-message-auto-dismiss-value="true"
           data-flash-message-delay-value="5000"
           class="bg-green-900 border border-green-700 text-green-100 px-4 py-3 rounded relative mx-4 mt-4 transition-all duration-300"
           role="alert">
        <div class="flex items-center justify-between">
          <span class="block sm:inline"><%= notice %></span>
          <button type="button"
                  data-action="click->flash-message#manualDismiss"
                  class="ml-4 text-green-200 hover:text-green-100 focus:outline-none">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div data-controller="flash-message"
           data-flash-message-auto-dismiss-value="true"
           data-flash-message-delay-value="8000"
           class="bg-red-900 border border-red-700 text-red-100 px-4 py-3 rounded relative mx-4 mt-4 transition-all duration-300"
           role="alert">
        <div class="flex items-center justify-between">
          <span class="block sm:inline"><%= alert %></span>
          <button type="button"
                  data-action="click->flash-message#manualDismiss"
                  class="ml-4 text-red-200 hover:text-red-100 focus:outline-none">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
      </div>
    <% end %>

    <!-- Main content -->
    <main class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
      <%= yield %>
    </main>
  </body>
</html>
